import React, {useState} from 'react';
import Header from './index'

interface indexProps {
    
}

const index = () => {
    return (
        <Header>
        </Header>
    );
}

export default index;